# op: issue
# keys: issue:{owner}/{repo}#{number}
query issue($owner: String!, $repo: String!, $number: Int!) {
  repository(owner: $owner, name: $repo) {
    issue(number: $number) { number title state id }
  }
  rateLimit { remaining resetAt cost }
}
